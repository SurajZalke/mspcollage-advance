specVersion: "v1"
serviceId: "msp-collage-advance"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "msp-collage-advance-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
